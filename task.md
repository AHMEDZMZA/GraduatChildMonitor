# Tasks - Fix Disappearing Articles State Bug

- `[x]` Modify `articles_view.dart` to add `buildWhen` to `BlocBuilder` and capture `ArticlesCubit` pre-async gap.
- `[x]` Modify `favourites_view.dart` to add `BlocListener` and `buildWhen` to prevent flashing/disappearing.
- `[x]` Run `flutter analyze` to verify the codebase compiles successfully.
